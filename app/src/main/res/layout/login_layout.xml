<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/colorPrimary"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:theme="@style/AppTheme.NoActionBar">

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:orientation="vertical">

        <ImageView
            android:layout_width="@dimen/imageview_width_login"
            android:layout_height="@dimen/imageview_height_login"
            android:layout_marginBottom="@dimen/activity_vertical_margin"
            android:src="@drawable/lodestar"
            android:layout_gravity="center"/>

        <EditText
            android:id="@+id/editText"
            style="@style/EditTextStyle"
            android:hint="name@gmail.com"
            android:inputType="textEmailAddress" />

        <EditText
            android:id="@+id/editText2"
            style="@style/EditTextStyle"
            android:hint="password"
            android:inputType="textPassword" />

        <Button
            android:id="@+id/button_login"
            style="@style/ButtonStyle"
            android:background="@drawable/button_login_bg"
            android:text="Login"
            android:textColor="@android:color/white" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/sign_up"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/margin_textview_signup"
                android:layout_marginLeft="@dimen/margin_textview_signup"
                android:layout_marginStart="@dimen/margin_textview_signup"
                android:layout_weight="1"
                android:text="@string/textview_sign_up"
                android:textColor="@android:color/white" />

            <TextView
                android:id="@+id/forgot_password"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/margin_textview_forgot_password"
                android:layout_marginEnd="@dimen/margin_textview_forgot_password"
                android:layout_marginRight="@dimen/margin_textview_forgot_password"
                android:layout_weight="1"
                android:gravity="end"
                android:text="@string/textview_forgot_password"
                android:textColor="@android:color/white" />
        </LinearLayout>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="or"
            android:textColor="@android:color/white" />

        <Button
            android:id="@+id/google_button"
            style="@style/ButtonStyle"
            android:background="@drawable/button_google_login_bg"
            android:text="Login with google"
            android:textColor="@android:color/white" />

        <Button
            android:id="@+id/facebook_button"
            style="@style/ButtonStyle"
            android:background="@drawable/button_facebook_login_bg"
            android:text="Login with Facebook"
            android:textColor="@android:color/white" />

    </LinearLayout>
</RelativeLayout>